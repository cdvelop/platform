//go:build wasm

package layout

// import (
// 	"github.com/tinywasm/dom"
// )

func (l *Layout) OnMount() {
	// Replicating modalHandler(event) logic if needed
	// modal, ok := dom.Get("modal-window")
	// if ok && modal != nil {
		// modal.AddEventListener("click", func(e dom.Event) {
		// 	dom.Log("Modal clicked")
		// })
	// }
}
